logging:
  version: 1

  root:
    level: INFO

  console:
    enabled: true
    level: INFO

  file:
    enabled: true
    level: DEBUG
    directory: logs
    filename: healthrisk.log

  formatter:
    format: "%(asctime)s | %(levelname)s | %(name)s | %(message)s"